let stock = {
  Fruits: ["strawberry", "banana", "apple", "grapes"],
  liquid: ["water", "ice"],
  holder: ["cone", "cup", "stick"],
  toppings: ["chocolate", "peanuts"],
};

let is_shop_open = true;

async function order() {
  try {
    await Abc;
  } catch (error) {
    return error;
  } finally {
    console.log("whatever");
  }
}

order();
//we can again make a promise chain from here
